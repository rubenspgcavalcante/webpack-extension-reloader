import {
  DEFAULT_PORT,
  DEFAULT_RELOAD_PAGE,
  DEFAULT_MANIFEST
} from "../constants/options.constants";

export default {
  reloadPage: DEFAULT_RELOAD_PAGE,
  port: DEFAULT_PORT,
  manifest: DEFAULT_MANIFEST
};
